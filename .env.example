# Set this to the SHA-256 hash of your desired password
# Generate with: node -e "const crypto = require('crypto'); console.log(crypto.createHash('sha256').update('YOUR_PASSWORD').digest('hex'))"
VITE_APP_PASSWORD_HASH=your_sha256_hash_here
